#!/usr/bin/env direnv

case "$(uname -a)" in
    *Microsoft*)     os="wsl";;
    *microsoft*)     os="wsl";;
    Linux*)     os="linux";;

    CYGWIN*)    os="windows";;
    MINGW*)     os="windows";;
    *Msys)     os="windows";;
    Darwin*)   os="darwin";;
esac

source_env "$PWD/$os"
